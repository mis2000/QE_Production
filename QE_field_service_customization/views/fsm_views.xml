<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_task_kanban_inherit" model="ir.ui.view">
        <field name="name">project.task.kanban</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_kanban"/>
        <field name="arch" type="xml">
            <field name="active" position="after">
                <field name="is_fsm"/>
            </field>
            <xpath expr="//div[hasclass('o_kanban_record_headings')]" position="replace">
                <div t-if="record.is_fsm.raw_value" class="o_kanban_record_headings">
                    <strong class="o_kanban_record_title">
                        <field name="partner_id"/><br/>
                    </strong>
                    <t t-if="record.partner_street.value">
                       Street: <field name="partner_street" /><br/>
                    </t>
                    <t t-if="record.partner_city.value">
                        City: <field name="partner_city" /><br/>
                    </t>
                    <strong class="o_kanban_record_title">
                        <field name="name"/>
                    </strong>
                    <span invisible="context.get('default_project_id', False) or context.get('fsm_mode', False)"><br/>
                        <field name="project_id"/>
                    </span>
                </div>
                <div t-else="" class="o_kanban_record_headings">
                    <strong class="o_kanban_record_title">
                        <field name="name"/>
                    </strong>
                    <span invisible="context.get('default_project_id', False) or context.get('fsm_mode', False)"><br/>
                        <field name="project_id"/>
                    </span>
                    <br/>
                    <t t-if="record.partner_id.value">
                        <span>
                            <field name="partner_id"/>
                        </span>
                    </t>
                    <t t-else="record.email_from.raw_value">
                        <span><field name="email_from"/></span>
                    </t>
                </div>
            </xpath>
        </field>
    </record>

    <record id="project_task_view_calendar_inherit" model="ir.ui.view">
        <field name="name">project.task.calendar</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="industry_fsm.project_task_view_calendar_fsm"/>
        <field name="arch" type="xml">
            <xpath expr="//calendar/field[@name='partner_id']" position="after">
                <field name="partner_street"/>
                <field name="partner_city"/>
                <field name="is_fsm"/>
            </xpath>
        </field>
    </record>

    <record id="project_task_view_gantt_inherit" model="ir.ui.view">
        <field name="name">project.task.calendar</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project_enterprise.project_task_view_gantt"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="partner_street"/>
                <field name="partner_city"/>
                <field name="name"/>
                <field name="is_fsm"/>
            </xpath>
            <xpath expr="//li[@t-if='partner_id']" position="after">
                <li t-if="is_fsm and partner_street"><strong>Street: </strong> <t t-esc="partner_street"/></li>
                <li t-if="is_fsm and partner_city"><strong>City: </strong> <t t-esc="partner_city"/></li>
                <li t-if="is_fsm and name"><strong>Task Name: </strong> <t t-esc="name"/></li>
            </xpath>
            <xpath expr="//li[@t-if='partner_id']" position="replace"/>
        </field>
    </record>

</odoo>
